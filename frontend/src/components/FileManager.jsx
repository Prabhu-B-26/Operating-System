import React from 'react';
import Terminal from './Terminal'; // Import the new component

const FileManager = () => {
  return (
    <div>
      <Terminal />
    </div>
  );
};

export default FileManager;